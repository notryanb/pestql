WHITESPACE = _{ " " }
keyword = { "select" | "from" }
selection = @{ (ASCII_ALPHANUMERIC | "_")+ ~ ("," ~ selection)* | "*" }
terminator = { ";" }
sql = { keyword ~ selection ~ keyword ~ selection ~ terminator }